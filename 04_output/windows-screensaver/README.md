# Gentle Drift for Windows

The Windows `.scr` release is built by GitHub Actions from `_src/windows-screensaver` and published as a GitHub Release when a version tag such as `v0.1.0` is pushed. The resulting ZIP contains `GentleDrift.scr`, its bundled `Web` directory, and installation instructions.
